# Frontend Architecture Guide (Архитектура Фронтенда)

This document outlines the frontend architecture for the IT Marketplace project. The structure is designed to align with the microservices backend, promoting scalability, maintainability, and clear separation of concerns.

## ⚠️ Ключевой принцип: Структура по фичам (Features)

Вместо того чтобы организовывать файлы по их техническому типу (например, все компоненты в `components`, все страницы в `pages`), мы структурируем их **по функциональным доменам (фичам)**. Фича — это логическая часть приложения, которая соответствует определенной бизнес-задаче или роли пользователя.

**Это главный принцип, который нельзя нарушать.**

---

## 🚀 Быстрый гид для разработчика: "Куда мне вносить изменения?"

-   **ЕСЛИ ВЫ РАБОТАЕТЕ НАД ЗАДАЧЕЙ, СВЯЗАННОЙ С:**
    -   Входом или регистрацией пользователя...
    -   **ВАША ПАПКА:** `src/features/1_Auth/`

-   **ЕСЛИ ВЫ РАБОТАЕТЕ НАД ЗАДАЧЕЙ, СВЯЗАННОЙ С:**
    -   Публичными страницами каталога (продукты, компании)...
    -   **ВАША ПАПКА:** `src/features/2_Marketplace/`

-   **ЕСЛИ ВЫ РАБОТАЕТЕ НАД ЗАДАЧЕЙ, СВЯЗАННОЙ С:**
    -   Регистрацией **Заказчика**...
    -   **ВАША ПАПКА:** `src/features/3_Customer/`

-   **ЕСЛИ ВЫ РАБОТАЕТЕ НАД ЗАДАЧЕЙ, СВЯЗАННОЙ С:**
    -   Регистрацией или личным кабинетом **Поставщика**...
    -   **ВАША ПАПКА:** `src/features/4_Supplier/`

-   **ЕСЛИ ВЫ РАБОТАЕТЕ НАД ЗАДАЧЕЙ, СВЯЗАННОЙ С:**
    -   Панелью **Оператора**...
    -   **ВАША ПАПКА:** `src/features/5_Operator/`

-   **ЕСЛИ ВЫ СОЗДАЕТЕ НОВЫЙ ПЕРЕИСПОЛЬЗУЕМЫЙ КОМПОНЕНТ (кнопка, инпут, карточка)...**
    -   **ВАША ПАПКА:** `src/shared/ui/`

---

## Структура папок

```
frontend/src/
├── app/                  # Глобальные настройки (главный компонент App, роутер, провайдеры)
├── shared/               # Переиспользуемый код (UI-кит, общие хуки, хелперы)
└── features/             # Функциональные модули (фичи) приложения
```

### `/app`
Здесь находится точка входа и глобальная конфигурация React-приложения.

-   `App.tsx`: Корневой компонент. Здесь "живут" глобальный `AuthContext`, основной роутер `<Routes>` и логика отображения модальных окон.
-   `App.css`: Глобальные стили.

### `/shared`
Библиотека общего кода, который можно использовать в **любой** фиче.

-   `/ui`: "Глупые" UI-компоненты без бизнес-логики.
    -   `Header.tsx`: Шапка сайта. Отображает разный контент в зависимости от авторизации.
    -   `Modal.tsx`: Базовый компонент модального окна.
-   `/lib`: Общие хуки, функции-хелперы (например, для форматирования дат).
-   `/assets`: Шрифты, иконки, глобальные изображения.

**Правило:** Если компонент или функция нужны в двух и более **разных** фичах, их место — в `shared`.

### `/features`
Сердце приложения. Каждая папка — это изолированный бизнес-домен.

#### `1_Auth` - Аутентификация
-   **Назначение:** Полностью отвечает за логику входа и регистрации.
-   **Ключевые файлы:**
    -   `components/Auth.tsx`: "Мозг" всего процесса. В зависимости от состояния, показывает либо форму входа, либо выбор роли, либо форму регистрации. Используется в модальном окне в `app/App.tsx`.
    -   `components/LoginForm.tsx`: Форма для входа пользователя.
    -   `components/AuthChoice.tsx`: Компонент, предлагающий выбор роли ("Заказчик" или "Поставщик") при регистрации.
-   **Связи:**
    -   Импортирует и отображает формы регистрации из `features/3_Customer` и `features/4_Supplier`.
-   **Важный архитектурный паттерн:** `Auth.tsx` получает функцию `onLoginSuccess` из `App.tsx` и передает ее дальше. Любой компонент, который выполняет вход пользователя (включая автоматический вход после регистрации), должен вызывать именно `onLoginSuccess`. Это гарантирует консистентное поведение (сохранение токена, редирект) во всем приложении.

#### `2_Marketplace` - Публичный маркетплейс
-   **Назначение:** Общедоступные страницы каталогов, которые видят все пользователи (включая неавторизованных).
-   **Ключевые файлы:**
    -   `pages/HomePage.tsx`: Главная страница сайта.
    -   `pages/ProductsPage.tsx`, `pages/CompaniesPage.tsx` и др.: Страницы-списки (реестры) продуктов, компаний и т.д.
-   **Связи:**
    -   Эти страницы напрямую подключены к роутеру в `app/App.tsx`.

#### `3_Customer` - Функционал Заказчика
-   **Назначение:** Всё, что специфично для роли "Заказчик".
-   **Ключевые файлы:**
    -   `components/RegisterCustomerForm.tsx`: Форма регистрации Заказчика.
-   **Связи:**
    -   Компонент используется фичей `1_Auth` в процессе регистрации.

#### `4_Supplier` - Функционал Поставщика
-   **Назначение:** Всё, что специфично для роли "Поставщик".
-   **Ключевые файлы:**
    -   `pages/ProfilePage.tsx`: Личный кабинет Поставщика.
    -   `components/RegisterSupplierForm.tsx`: Форма регистрации Поставщика.
    -   `components/CompanyProfileForm.tsx`: Форма редактирования данных компании в личном кабинете.
    -   `components/SupportSurvey.tsx`: Опросник о мерах поддержки, который является частью регистрации Поставщика.
-   **Связи:**
    -   `RegisterSupplierForm.tsx` используется фичей `1_Auth`.
    -   `ProfilePage.tsx` подключается к роутеру в `app/App.tsx`.

#### `5_Operator` - Панель Оператора
-   **Назначение:** Полностью изолированный раздел для управления платформой.
-   **Ключевые файлы:**
    -   `pages/OperatorPage.tsx`: Основная страница панели, управляющая вкладками.
    -   `components/TabSupport.tsx`, `components/TabRegistry.tsx` и др.: Компоненты, представляющие содержимое каждой вкладки.
    -   `components/ConfirmSupportModal.tsx`: Модальное окно для подтверждения мер поддержки.
-   **Связи:**
    -   `OperatorPage.tsx` подключается к роутеру в `app/App.tsx` и доступна только пользователям с ролью `operator`.

---

## 🚨 Главные правила для разработчиков

1.  **СТРОГАЯ ИЗОЛЯЦИЯ ФИЧ.** Запрещены перекрестные импорты. Код из `features/4_Supplier` **НИКОГДА** не должен импортировать что-либо напрямую из `features/5_Operator`.
2.  **ДЕЛИТЕСЬ КОДОМ ЧЕРЕЗ `shared`.** Если вам нужен компонент или функция в нескольких фичах — выносите их в папку `shared`.
3.  **НОВАЯ ФИЧА — НОВАЯ ПАПКА.** При добавлении нового крупного раздела (например, "Корзина заказов") создайте для него новую папку в `features`.

---

## 💡 Паттерны и лучшие практики

### Общение между компонентами (State Lifting)

В приложении часто возникает задача, когда действие в одном компоненте должно вызвать обновление в другом "соседнем" компоненте. Например, успешное сохранение данных в модальном окне (`ConfirmSupportModal`) должно обновить статистику в основной вкладке (`TabSupport`).

**Проблема:** Эти компоненты напрямую не связаны.

**Решение:** Мы используем паттерн **"Поднятие состояния" (State Lifting)**.

1.  **Общий родитель:** Логика управления выносится в ближайшего общего родителя. В нашем случае это `OperatorPage.tsx`.
2.  **"Триггер" в родителе:** В `OperatorPage` создается состояние-триггер (например, `const [refreshTrigger, setRefreshTrigger] = useState(0)`).
3.  **Передача колбэка:** Компонент, инициирующий действие (`ConfirmSupportModal`), получает из родителя функцию-колбэк (`onSuccess`), которая изменяет этот триггер.
4.  **Передача триггера:** Компонент, который должен обновиться (`TabSupport`), получает сам триггер (`refreshTrigger`) в качестве пропа и добавляет его в массив зависимостей своего `useEffect`.

**Результат:** Когда модальное окно вызывает `onSuccess`, состояние в родителе меняется, `useEffect` в компоненте со статистикой перезапускается, и данные запрашиваются заново. Этот паттерн обеспечивает предсказуемый и управляемый поток данных.

---

## 🎨 Управление стилями (CSS)

Чтобы избежать конфликтов и поддерживать порядок, мы используем трехуровневую систему стилизации.

### 1. Глобальные стили
-   **Где:** `src/index.css` и `src/app/App.css`
-   **Назначение:**
    -   `index.css`: Стили для `body`, `html`, CSS-переменные (цвета, шрифты), сброс стандартных отступов (`margin`, `padding`).
    -   `App.css`: Стили для самого общего макета приложения, например, для класса `.app-container`.
-   **Правило:** Используйте эти файлы только для самых общих стилей, которые должны применяться ко всему приложению. **Не добавляйте сюда стили для конкретных компонентов.**

### 2. Стили для переиспользуемых компонентов
-   **Где:** Рядом с компонентом в `src/shared/ui/`, например, `Header.css` для `Header.tsx`.
-   **Назначение:** Стили, которые неразрывно связаны с их компонентом и "путешествуют" вместе с ним.
-   **Правило:** Стилизуйте только классы внутри вашего компонента. Старайтесь не использовать селекторы по тегам (например, `div`, `span`), чтобы не затронуть вложенные компоненты.

### 3. Стили для компонентов фичи
-   **Где:** Рядом с компонентом внутри папки фичи, например, `src/features/5_Operator/pages/OperatorPage.css`.
-   **Назначение:** Стили, которые нужны **только** для этой конкретной фичи.
-   **Правило:** Эти стили должны быть максимально инкапсулированы. Если вы замечаете, что пишете одинаковые стили для компонентов в разных фичах, подумайте о том, чтобы вынести этот компонент в `shared/ui`.
